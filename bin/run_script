#!/usr/bin/env bash
run_script_path="$(dirname "$0")/run_script.py"
if [[ "$(uname -o)" == "Msys" ]]; then
    run_script_path="$(cygpath -w "${run_script_path}")"
fi
python3 "${run_script_path}" "$@"
