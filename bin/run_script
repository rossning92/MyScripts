#!/usr/bin/env bash
run_script="$(dirname "$0")/run_script.py"
if [[ "$(uname -o)" == "Msys" ]]; then
    run_script="$(cygpath -w "${run_script}")"
fi

if [ -x "$(command -v python3)" ]; then
    python3 "${run_script}" "$@"
else
    python "${run_script}" "$@"
fi
