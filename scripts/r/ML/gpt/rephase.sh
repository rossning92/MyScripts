run_script r/ML/gpt/chatgpt.py --copy-to-clipboard --adhoc "Rephase the following, use proper english, don't use fancy word, simple and concise but still keep all meaning, don't replace the important keyword that may change the meaning. If i'm using markdown, keep the format don't change:" "$1"